import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LabMobile6_RegitaMaulia_C',
  webDir: 'www'
};

export default config;
